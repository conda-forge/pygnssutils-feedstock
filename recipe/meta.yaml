{% set name = "pygnssutils" %}
{% set version = "1.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pygnssutils-{{ version }}.tar.gz
  sha256: 7ad764a3cb812477eef91f6cadf4f72cd17d37f0c0535d5e89efb28b55a69cb9
build:
  entry_points:
    - gnssstreamer = pygnssutils.gnssstreamer_cli:main
    - gnssserver = pygnssutils.gnssserver_cli:main
    - gnssntripclient = pygnssutils.gnssntripclient_cli:main
    - gnssmqttclient = pygnssutils.gnssmqttclient_cli:main

  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=75.0.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - certifi >=2025.0.0
    - paho-mqtt >=2.1.0
    - pyserial >=3.5
    - pyspartn >=1.0.8
    - pyubx2 >=1.3.3
    - pysbf2 >=1.0.4
    - pyunigps >=1.0.0
    - pyqgc >=0.2.2
    - pynmeagps >=1.1.4
    - pyubxutils >=1.0.6

test:
  imports:
    - pygnssutils
  commands:
    - pip check
    - gnssstreamer --help
    - gnssserver --help
    - gnssntripclient --help
    - gnssmqttclient --help
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/semuconsulting/pygnssutils
  description: Python GNSS CLI utility library.
  summary: Python GNSS CLI utility library.
  license: BSD-3-Clause
  license_file: LICENSE
  doc_url: https://www.semuconsulting.com/pygnssutils
  dev_url: https://github.com/semuconsulting/pygnssutils

extra:
  recipe-maintainers:
    - semuadmin
